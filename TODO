player authentication

game history in git repo
* commit on move
* allow forking of game through UI
* game history as submodule?
* or just a config file that points to a git repo URL / commit ?
* bootstrapping to current state from game history
* current ruleset should just be a text document on each commit
* repo of game templates
